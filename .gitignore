dist/
node_modules/
coverage/
.env
supabase/.branches/
supabase/.temp/

# Per-checkout CodeGraph index. Its root and indexed bytes are specific to
# this working tree, so it must never be shared between checkouts.
.codegraph/

# Coding-agent and MCP scratch directories. They are tool state, not source,
# and prettier scans everything it is not told to skip.
.atl/
.playwright-mcp/
